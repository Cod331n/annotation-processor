plugins {
    id("java")
}

group = "net.implario"
version = "1.0-SNAPSHOT"

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(platform("org.junit:junit-bom:5.9.1"))
    testImplementation("org.junit.jupiter:junit-jupiter")

    implementation files('libs/cofoja-contracts-1.3.jar')
}

tasks.withType(JavaExec).configureEach {
    jvmArgs "-javaagent:./libs/cofoja-contracts-1.3.jar"
}


tasks.test {
    useJUnitPlatform()
}